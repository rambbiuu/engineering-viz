# Audit — engineering-viz tools

Scope: all 15 files in `tools/` plus `index.html`, against `.research/STYLE.md`.
Every claim below was checked by reading the file; the arithmetic ones were re-run
verbatim in node (`group()`, `Jnu`, `filtAt`, `folded`, `loss`, `gasAtten`, the
epicycle radii, the link-budget axis, and the canvas rotation algebra).

Totals: **8 critical, 27 major, 24 minor.**

---

## Critical

| # | File | Category | Exact line / function | What is wrong | Fix |
|---|------|----------|----------------------|---------------|-----|
| C1 | `cellular-reuse.html` | physics | line 123 `let g = (a * (i + 2 * j) - b * i) % S.N;` | The co-channel group map is wrong for 4 of the 6 presets. Ran verbatim: **N=3** puts the same channel on the *immediately adjacent* cell (nearest co-channel D = 1.73R, card claims 3.00R); **N=4** likewise (D = 1.73R vs 3.46R claimed) and paints only **2 colours instead of 4**; **N=9** paints 3 colours, D = 3.00R vs 5.20R; **N=12** paints 6 colours, D = 3.46R vs 6.00R. Only N=1 and N=7 are correct (N=7 by luck — it lands on the mirror-image lattice). The tool's entire subject is "how far apart two cells must be", and it draws that distance wrong on 4 clicks out of 6, while the card next to it prints the correct √(3N). | The label must be the coset index of `(a,b)` in Z²/L, L = span{(i,j), (−j,i+j)}. For gcd(i,j)=1 (N=1,3,7) use `g = ((i+j)*a + j*b) % N` — verified to kill both generators. N=4, 9, 12 have gcd(i,j)>1, so Z²/L is **not cyclic** and no single linear form works; reduce (a,b) against the two basis vectors and index the coset (or drop those three presets). |
| C2 | `antenna-basics.html` | rendering | lines 243 and 261 `g.rotate(rad(90));` vs line 350 `g.lineTo(Math.sin(rad(S.mech)) * bl, -Math.cos(rad(S.mech)) * bl);` | Section 1 draws three things that should agree in three directions 90° apart. At `mech = 0`: the **hardware** is drawn along local +x → screen **right**; the **radiation lobe** at a=0 goes through `rotate(+90°)`, which maps (1,0)→(0,1) → screen **down**; the **"mechanical boresight" dashed line** is (sin 0, −cos 0) → screen **up**. So the Yagi points right, radiates downward, and its boresight arrow points up. This is visible in every state of the tool, and section 1 is the panel the title promises ("the hardware, the radiation it throws out"). | Pick one convention and use it everywhere. Cheapest: delete both `g.rotate(rad(90))` calls (pattern a=0 → +x, matching the hardware) and change line 350 to `g.lineTo(Math.cos(rad(S.mech)) * bl, Math.sin(rad(S.mech)) * bl);`. Section 2's polar plot already uses 0°=up consistently and needs no change. |
| C3 | `superhet-image.html` | physics | lines 116–119 `const filtAt = f => { const off = Math.abs(f - S.frf) / (S.bw / 2); return off <= 1 ? 0 : -12 * Math.pow(off, 1.7); };` | An invented power law with two unexplained constants, driving the headline "Image rejection" card. No real filter rolls off as `off^1.7`; an n-pole filter is `10n·log10(1+off^2n)`, i.e. linear in log(off). Measured verbatim: at the defaults (off = 3.57 half-widths) the tool claims **104 dB** where a real 2-pole gives 44 dB and a 3-pole 99 dB; at fRF=400/IF=70/bw=12 it returns **2539 dB**, printed as "> 60 dB". A 12 MHz-wide front-end filter cannot do 104 dB at 21 MHz offset. | Replace with a stated filter order: `const N = 3; return -10 * N * L10(1 + Math.pow(off, 2 * N));` and say "3-pole Butterworth" in the section caption or the closing paragraph. Then `rejection()` is a number an engineer can check. |
| C4 | `radio-propagation.html` | physics | lines 386–396, `const loss = d => {...}`; specifically `extra = Math.min(28 + 0.55 * beyond * (fMHz() > 1000 ? 1.5 : 1), 95);` and `extra = Math.min(extra, scFloor - fs + 30);` | The whole beyond-horizon model is fabricated, and section 2 sells it as "the classic three-region picture". Diffraction excess is a flat 28 dB the instant you cross the horizon plus 0.55 dB/km — verified: `loss(hor−0.01) = 96.2 dB`, `loss(hor+0.01) = 124.2 dB`, a **28 dB vertical cliff** in the drawn curve at the horizon marker. Real spherical-earth diffraction rises continuously from ≈6 dB at grazing and scales as f^(1/3), not as a frequency step at 1 GHz. `scFloor − fs + 30` has no derivation; the `+30` is a free parameter. | Either implement the ITU-R P.526 spherical-earth diffraction term (continuous at grazing) plus a P.617 troposcatter loss, or keep the cartoon and label it: rename the section "shape of the three regions, schematic" and put the disclosure in the closing paragraph as STYLE §Physics requires. At minimum make `extra` start at ~6 dB and ramp so the cliff disappears. |
| C5 | `epicycles.html` | rendering | line 125 `const scale = Math.min(cx - 24, h / 2 - 24) / 1.35;` | The `/1.35` budget assumes the epicycle chain never extends further than 1.35 units, but the chain length is Σ\|aₙ\|, not the wave amplitude. Verified: square at the default N=4 gives Σ\|aₙ\| = 2.134 → the chain reaches **215 px** from cx=160, i.e. left edge x = **−55** (off-canvas) and right edge x = **375**, while the waveform area starts at x = 340. At N=12 it is x = −125 to x = 445. The circle stack is clipped by the canvas edge and draws on top of the trace, once per revolution. Square is one click from the default. | Normalise by the actual chain length: `const sum = terms().reduce((s, t) => s + Math.abs(t.a), 0); const scale = Math.min(cx - 24, h / 2 - 24) / Math.max(sum, 1);`. `terms()` is already available at that point in `draw()`. |
| C6 | `index.html` | pedagogy | lines 61–117, `<div class="grid">…</div>` | Four finished tools are unreachable. The grid links 11 files; `tools/` contains 15. Missing: **`cellular-reuse.html`, `fresnel-clearance.html`, `link-budget.html`, `superhet-image.html`** — including `link-budget.html`, which STYLE §Reference implementation names as the file every other tool is copied from. All four carry a working "← all tools" back link into an index that never links to them. | Add four `<a class="card">` entries. Tags to match the existing set: `RF · link budget`, `RF · path planning`, `Comms · network planning`, `RF · receivers`. |
| C7 | `phase-interferometry.html` | numerics | line 134 `let sinEst = Math.max(-1, Math.min(1, q[0] * L / (360 * d[0])));` | `d[0]` is antenna A2's position, an `<input type="number" min="0">` the user can legitimately set to 0 (or blank — line 125 coerces to `0`). Then `q[0]` is also 0 and the expression is `0/0`. Ran verbatim with pos = [0,0,10,30]: `sinEst = NaN`, and the headline readout `$('res').textContent = sv.th.toFixed(1) + '°'` renders **"NaN°"**, with every step card showing "NaN°" too. STYLE hard rule: "No `NaN`… reaching the DOM — guard every division and `asin`." | Guard the baseline, not the result: in `solve()` filter out zero-length baselines first — `const d = S.pos.slice(1).filter(p => Math.abs(p) > 1e-6)` (and slice `q` to match), returning `null` if none remain. `updateUI` already handles `sv === null`. |
| C8 | `sampling-aliasing.html` | physics | line 117 `const ok = S.f <= S.fs / 2;` | Gets the Nyquist boundary — the one fact the tool exists to teach — backwards at the boundary. Nyquist requires fs > 2f **strictly**. Verified at f=10, fs=20 (defaults, one drag of the frequency slider): every sample value is exactly 0.000, no ghost is drawn, and the card prints *"correct — the sample rate is fast enough"* over a row of dots that carry no information at all. | `const ok = S.f < S.fs / 2;` and add the equality case to the footnote: at exactly fs/2 the samples can land on the zero crossings and the amplitude is lost, which is why the rule is strict. The `aliased` test on line 155 needs the same treatment or the ghost still will not draw. |

---

## Major

| # | File | Category | Exact line / function | What is wrong | Fix |
|---|------|----------|----------------------|---------------|-----|
| M1 | `fresnel-clearance.html` | physics | line 120 `if (v <= -0.7) return 0;` and lines 249/256/275 `const loss = cf > 0.577 ? 0 : Jnu(nu());` | Two magic numbers, neither matching the ITU-R P.526 knife-edge model the rest of the function is taken from. P.526 is valid for ν > −0.78; the code cuts at ν = −0.70, where J(ν) is still **0.536 dB** — verified, so the curve has a **step discontinuity** from 0.55 dB to 0 at cf = 0.495 F₁. The separate `cf > 0.577` gate corresponds to ν = −0.816 and has no derivation at all. | `if (v <= -0.78) return 0;` (J(−0.78) = 0.004 dB, continuous), and delete the `cf > 0.577 ? 0 :` gate entirely — `Jnu` already returns 0 past its own validity limit. |
| M2 | `fresnel-clearance.html` | physics / pedagogy | line 96 caption `— the knife-edge curve; grazing the obstacle already costs 6 dB, and excess clearance rings rather than settling` | The caption promises the Fresnel ripple, and the code makes it impossible: `loss = cf > 0.577 ? 0 : …` forces exactly 0 dB for all clearance above 0.577 F₁, so the drawn curve is dead flat to the right and **never rings**. The prose describes behaviour the reader cannot see. | Fixing M1 is necessary but not sufficient — P.526's J(ν) is monotone and also does not ripple. Either plot the exact Fresnel-integral result (which does oscillate about 0 dB for ν < −1) or delete "and excess clearance rings rather than settling" from the caption. |
| M3 | `fresnel-clearance.html` | rendering | line 228 `g.fillText((v === 0 ? '0' : '−' + v) + ' dB', l - 6, Y(v));` | The loop starts at `v = -6`, so the top gridline renders the literal string **"−-6 dB"** (verified). | Format the sign from the value: `g.fillText((v > 0 ? '−' : '') + Math.abs(v).toFixed(0) + ' dB', l - 6, Y(v));`, or start the loop at `v = 0`. |
| M4 | `fresnel-clearance.html` | pedagogy | `drawProfile()`, lines 132–212 | The path-profile canvas has **no vertical axis at all** — no height gridlines, no metre labels — although `const l = 44` reserves the left margin for one. The user cannot read the height of the obstacle, the masts, the bulge, or the Fresnel radius off the picture that is the whole tool. STYLE §Physics: "Units on every axis and every card." | Add the y ticks in the reserved margin: 4–5 gridlines from 0 to `top`, labelled `v.toFixed(0) + ' m'`, using the same pattern as the existing x loop at lines 144–149. |
| M5 | `fresnel-clearance.html` | rendering | lines 167–178, the `losHeight(x) ± F1(x, S.d - x)` fill and stroke | The Fresnel ellipse is drawn unclipped. At f = 0.05 GHz, d = 80 km, maxF = 346 m against masts of 60 m, so the lower edge sits at −286 m; `Y()` maps that below `h - b` and the blue fill floods the x-axis label band, over the "0 km / 20 km …" ticks. | Wrap the ellipse drawing in `g.save(); g.beginPath(); g.rect(l, t, w - l - r, h - t - b); g.clip();` … `g.restore();` — the same clip pattern already used in `phase-interferometry.html` line 218. |
| M6 | `phased-array.html` | pedagogy | line 85 — the file's first body element is `<h1>Phased-array beam pattern</h1>` | The only tool in the suite with **no back link**. STYLE §Required structure item 3 mandates `<p style="margin: 0 0 1rem;"><a class="back" href="../index.html">&larr; all tools</a></p>` as the first element, and there is no `.back` rule in its `<style>` either. Confirmed by grep: 14/15 files have it, this one has 0. | Add the back paragraph before `<h1>`, and the two `.back` rules from the reference file. |
| M7 | `phased-array.html` | style / rendering | line 138 `const COLS = ['#888', '#7F77DD', '#1D9E75', '#D85A30'];` | `#888` is a raw grey hex, banned outright by STYLE §Hard rules ("Never a raw grey/black hex for text"), and it is used as text colour on line 186 `el.style.color = COLS[i]` for the A1 feed-phase chip and as the A1 element dot. It is hardcoded, so it does not follow the dark-mode token flip: contrast against `--bg: #1b1b19` is poor. Grep confirms this is the only raw grey in the suite. | Read the token at draw time like every other file does: replace the literal with `cvv('--text-primary')` inside the drawing/labelling code (the array is built before `cvv` is used, so make `COLS` a function or index into `[null, …]` and substitute at use). |
| M8 | `antenna-basics.html` | physics | lines 139–156, the `TYPES` array `gain:` fields, versus `patt()` lines 158–196 | Gain is a hardcoded lookup table with no connection to the pattern the tool actually draws, so the two headline cards contradict each other. The dish is declared 34 dBi, but its pattern uses `D = 11` wavelengths, whose −3 dB width is 4.6° in the drawn cut → ≈31.6 dBi by G ≈ 31000/(θ_az·θ_el), and whose aperture gain is η(πD/λ)² ≈ 28 dBi at 55% efficiency. The horn is declared 17 dBi with a `D = 3.2` pattern that is far too narrow for it. The single most useful antenna relation — gain ↔ beamwidth — is exactly what the tool hides. | Derive gain from the drawn pattern instead of tabulating it: integrate `patt()` for directivity, or at minimum compute `G ≈ 10*L10(31000 / (hpbw * hpbw_assumed_other_plane))` and show the aperture size used. If the table stays, state in the closing paragraph that gains are typical catalogue figures, not computed from the plotted cut. |
| M9 | `antenna-basics.html` | physics | lines 177–182, the `t === 'yag'` branch: `const main = Math.pow(Math.max(ca, 0), 9); const back = 0.09 * Math.pow(Math.max(-ca, 0), 3); const side = 0.11 * Math.abs(Math.sin(3.4 * a)); return Math.max(main, Math.max(back, side * Math.pow(Math.max(ca, 0), 0.6)));` | A plausible-looking curve assembled from three unrelated ad-hoc expressions with five invented constants (9, 0.09, 3, 0.11, 3.4, 0.6), and combined with `Math.max` rather than by summing fields. Consequences: the front-to-back ratio card reads out `20·log10(1/0.09) = 20.9 dB` — a number typed in, not computed from any array; there are **no nulls** anywhere in the pattern because `max()` cannot produce them, so the "genuine null" the dipole note celebrates is structurally absent from the Yagi; and sidelobes exist only in the forward hemisphere because of the `cos^0.6` factor. | A Yagi is a small end-fire array — build it as one: sum the element fields with the usual reflector/director phase offsets, `Math.hypot(Σcos, Σsin)`. That yields real nulls, a real F/B, and a real beamwidth from one model. If that is too much, say in the note that the pattern is a sketch. |
| M10 | `antenna-basics.html` | pedagogy | line 78 `<button id="t0" class="on">Isotropic</button>` and line 134 `const S = { t: 0, … }` | The default state is the one antenna with nothing to look at: a featureless circle, and three of the four cards read "none", "0 dB" and "no beam". STYLE §Physics: "the default state must already show something worth looking at." The tool's own closing paragraph tells the reader to go and click Array. | Default to `t: 4` (Yagi) or `t: 7` (Array with a small `elec` offset) — Array at elec = 20° immediately demonstrates the mechanical-vs-electrical boresight split the closing paragraph is about. Keep Isotropic as a click. |
| M11 | `antenna-basics.html` | style / accessibility | line 258 `const wob = (time * 0.0012) % 1;` driving lines 258–273, and line 481 `function frame(ts) { drawPhys(ts); drawPolar(); requestAnimationFrame(frame); }` | This file genuinely animates (expanding radiation rings) and is the one animated tool with **no** `matchMedia('(prefers-reduced-motion: reduce)')` check and **no** pause button. STYLE §Hard rules requires respecting reduced motion. Grep: only `epicycles`, `fourier-series`, `iq-constellation`, `phase-interferometry` honour it. | Copy the pattern from `iq-constellation.html` lines 123–124 and 241–245: a `S.playing` flag initialised from the media query plus a Pause button, and freeze `wob` when paused. |
| M12 | `radio-propagation.html` | physics | lines 180–185 `function gasAtten(fGHz)`, specifically the term `0.9 / (1 + Math.pow((fGHz - 183) / 9, 2))` | The 183 GHz water line peaks at **1.49 dB/km** in this model against ≈28–30 dB/km in ITU-R P.676 at sea level — 20× low — and the curve is captioned "standard ITU-shaped absorption" in the closing paragraph. Also verified: 22.2 GHz reads 0.507 vs ≈0.2 (2.5× high), 118.75 GHz reads 0.79 vs ≈1.5–2, and 300 GHz reads 1.18 vs ≈10–30. The 60 GHz peak (14.15 vs ≈15) is the only accurate point. The chart's whole job is the shape, and above 100 GHz the shape is wrong. | Raise the 183 GHz line strength to ≈28 and the 118 GHz to ≈1.8, and add the dry-air continuum growth above 100 GHz (P.676 Annex 2 gives a closed form). Or restrict the x-axis to 1–100 GHz, where the current fit is defensible, and say so. |
| M13 | `radio-propagation.html` | physics | lines 148–152 `function groundWaveKm() { … return Math.min(2500, 900 / Math.pow(Math.max(f, 0.003), 0.55)); }` | A wholly invented expression feeding the "Practical reach" card as a hard number in km. At 1 MHz it claims 900 km; real MF ground wave is ~100–200 km over land and ~500–1000 km over good seawater, and the difference between land and sea is the single most important ground-wave fact. Neither the exponent 0.55 nor the constant 900 is disclosed anywhere. | Either use the Norton/ITU-R P.368 ground-wave curves (a small lookup table of f × conductivity is enough), or add a land/sea conductivity control and state the fit in the closing paragraph. At minimum change the card footnote to say the figure is indicative. |
| M14 | `radio-propagation.html` | rendering | line 437 `g.fillText('free space only', X(dmax * 0.55), Y(loss(dmax * 0.55)) - 30);` | The label for the **dashed free-space curve** is positioned using `loss()`, the *total* curve. Beyond the horizon those differ by up to 95 dB, so the label floats far from the line it names and lands on or near the solid blue curve instead — actively mislabelling the wrong line. | `Y(32.44 + 20 * L10(Math.max(fMHz(), 0.01)) + 20 * L10(dmax * 0.55)) - 6` — the same expression the dashed curve itself uses on line 432. |
| M15 | `radio-propagation.html` | pedagogy | `drawPic()` line 372 `g.fillText('distance from transmitter →', w - 8, h - 6);` | Section 1 has **no distance scale whatsoever** — no ticks, no km, and the drawn earth curvature (`const ER = w * 3.4`, line 228) bears no relation to the real horizon. The horizon marker position is `Math.min(0.42, horizonKm() / 260)` (line 313), which saturates at 109 km, so above roughly 175 m mast height the marker stops moving while the "Radio horizon" card keeps changing. Reader cannot connect the picture to the numbers. | Label the axis: pick a fixed span (say 0–400 km), tick it, and place the horizon marker at `horizonKm()/span` with the ionosphere layer heights labelled in km. If the panel is meant to be schematic, say "not to scale" on it. |
| M16 | `superhet-image.html` | numerics / physics | line 115 `const fimg = () => S.hi ? S.frf + 2 * S.fif : S.frf - 2 * S.fif;` | With low-side LO the image frequency goes negative and is printed as a frequency. Slider extremes fRF = 80 MHz, IF = 70 MHz give **image = −60.00 MHz** in the "Image frequency" card, a labelled `−60.0` spike on the RF canvas, and a "2 × IF" measuring bar drawn across a negative-frequency region. Physically, once 2·IF > fRF the low-side image has folded through DC and the single-conversion assumption has failed. | Guard it: when `fimg() <= 0`, print "none — 2×IF exceeds the RF frequency" in the card, skip the image spike, and grey the low-side button. That is also the pedagogically interesting case and worth surfacing rather than hiding. |
| M17 | `superhet-image.html` | physics / pedagogy | lines 240–243 `{ f: 2 * f1 - f2, lab: '2f₁−f₂', lvl: -18, ord: 3 }, … { lvl: -38, ord: 5 }` | Intermod product levels are hardcoded constants that never move. The heading is "Intermodulation — when two strong signals make a third" and the closing paragraph is about front-end overload, yet the tool cannot show the one law that matters: **IM3 rises 3 dB for every 1 dB of input**, which is why intermod is a *level* problem. The "Interferer level" slider exists and drives only the image branch. | Drive the products from an input level and a stated IP3: `lvl3 = 3 * Pin - 2 * IP3` and `lvl5 = 5 * Pin - 4 * IP5`, with `Pin` from the existing slider. Two lines, and the section then teaches its own title. |
| M18 | `superhet-image.html` | pedagogy / style | lines 56–58 readouts `<span class="v" id="frv">160.7</span>`, `id="fiv">10.7`, `id="bwfv">12`, and `updateUI()` lines 265–267 | Three of the four slider readouts print **bare numbers with no unit**. The only mention of MHz is a muted note parked in a different row (line 65). STYLE §Physics: "Units on every axis and every card." | Append `' MHz'` in `updateUI()` for `frv`, `fiv` and `bwfv`, and drop the orphaned note. |
| M19 | `modulation.html` | pedagogy | `syncControls()` lines 302–314, specifically `if (+$('dev').value > 100) $('dev').value = 80;` | Switching AM → FM → AM leaves AM at **6 % depth**. Trace: AM starts at 80; clicking FM hits `if (+$('dev').value > 10) $('dev').value = 6`; clicking AM back only clamps `> 100`, which 6 is not, so it stays. The AM envelope is then almost invisible and the tool looks broken, on the default scheme, after two clicks. The value is also off the `step = 5` grid. | Store the two values separately (`S.depthAM`, `S.devFM`) and restore the right one in `syncControls()`, rather than clamping a shared slider. |
| M20 | `aoa-triangulation.html` | pedagogy | intro line 71 "*lines rarely agree perfectly, so the answer becomes a best fit rather than a single crossing*" vs `draw()` lines 138–158 | No measurement error is ever realised. `bearing(st)` (line 113) returns the **exact** true bearing to the emitter for every site, so all wedges are perfectly centred on the emitter and all centre lines meet exactly at one point no matter how many sites you add. The "best fit rather than a single crossing" the intro promises, and the "adding a third, fourth or fifth site shrinks the overlap region" the closing paragraph promises, are both invisible — extra sites just add more lines through the same point. There is also no computed fix, no error ellipse and no CEP. | Draw one *realisation*: give each site a fixed pseudo-random offset within ±`S.err` (seeded per site, re-rolled by a "New measurement" button), draw the measured bearings, and compute the least-squares intersection plus the residual. That single change makes both prose claims true and costs about 15 lines. |
| M21 | `tdoa.html` | pedagogy | closing paragraph line 87 "*it demands that the sites share clocks synchronized to nanoseconds (1 µs of clock error = 300 m of position error)*" | Same defect as M20: `aS = (dist(E,F1) - dist(E,F2)) / 2` (line 162) is the exact geometric value, so every hyperbola passes exactly through the emitter and they always cross at one point. The tool asserts the headline sensitivity — 1 µs → 300 m — and provides no way to see it. It also has no timing-jitter control, unlike its sibling `aoa-triangulation.html`, which at least has a bearing-accuracy slider. | Add a "clock error" slider in ns, perturb each site's Δt by it, and shade the resulting hyperbola band. The 300 m/µs claim then becomes something the reader measures rather than reads. |
| M22 | `link-budget.html` | numerics / rendering | line 201 `const lo = thr - 45, hi = prx(dmin) + 5;` | No guarantee that `hi > lo`. Verified at reachable slider extremes (Tx −10 dBm, 40 GHz, 0 dBi both ends, 15 dB feeder, 30 dB misc, NF 12, BW 20 MHz, C/N 30): `lo = −103.99`, `hi = −140.46` → the y-axis is **inverted**, the gridline loop `for (let v = Math.ceil(lo/20)*20; v < hi; v += 20)` executes **0 times** so the chart loses all its labels, and the "you are here" marker leaves the canvas. | Order them: `const lo = Math.min(thr - 45, prx(dmax) - 5), hi = Math.max(thr + 10, prx(dmin) + 5);` — that also keeps the curve's far end on screen, which the current code does not. |
| M23 | `link-budget.html` | style | line 262 `$('cM').style.color = fm >= 10 ? 'var(--text-good)' : (fm >= 0 ? 'var(--text-warning)' : 'var(--text-warning)');` | Both branches of the inner ternary are identical, so a link that **fails** (negative margin) is coloured exactly like one that merely has thin margin. The card's own footnote distinguishes the two cases in words; the colour does not. In the reference implementation, which every other tool is told to copy. | The `--text-*` token set has no error colour; use the coral accent for failure: `: (fm >= 0 ? 'var(--text-warning)' : '#D85A30')`. |
| M24 | all 15 tools | style / accessibility | e.g. `link-budget.html` line 291 `function frame() { drawWF(); drawDist(); requestAnimationFrame(frame); }` | Every file redraws every canvas at 60 fps forever, including the **eleven** with no animation at all (`link-budget`, `fresnel-clearance`, `cellular-reuse`, `superhet-image`, `tdoa`, `aoa-triangulation`, `phased-array`, `sampling-aliasing`, `modulation`, `iq-constellation`'s static case, `antenna-basics`' polar panel). The worst is `antenna-basics`, where each frame calls `metrics()` twice and `metrics()` runs `peakDir()` — 1440 `patt()` evaluations — so it burns roughly 250k pattern evaluations per second to redraw an unchanging picture. Battery and fan cost with zero benefit. | For the static tools, call `draw()` from the input handlers and on `resize`, and delete the rAF loop. For the animated ones keep rAF but gate it on `S.playing`. Cache `metrics()` in `antenna-basics` — it only depends on `S`. |
| M25 | 11 of 15 tools | style / accessibility | grep: `prefers-reduced-motion` appears only in `epicycles`, `fourier-series`, `iq-constellation`, `phase-interferometry` | STYLE §Hard rules requires respecting `matchMedia('(prefers-reduced-motion: reduce)')`. `antenna-basics` and `radio-propagation` both animate continuously (`wob` rings; `g.lineDashOffset = -dash` marching ants) and honour neither the media query nor offer a pause control. | Add the two-line pattern from `iq-constellation.html` to both animated files; for the static ones M24's fix removes the issue entirely. |
| M26 | 3 tools | style | `fourier-series.html` line 260 `if (S.playing) animT += ts - last;`; `iq-constellation.html` line 251 `spin += (ts - last) / 1000 * 2 * Math.PI * S.fro;` | The frame delta is unclamped, against the explicit STYLE rule "clamp the delta (`Math.min(ts - last, 50)`)". After a backgrounded tab, `ts - last` can be seconds; the constellation jumps a large random rotation and the Fourier trace jumps phase. `epicycles.html` line 192 and `phase-interferometry.html` line 402 do it correctly and are the model. | `Math.min(ts - last, 50)` in both. |
| M27 | `sampling-aliasing.html` | pedagogy | line 103 `const S = { f: 3, fs: 20 };` and closing paragraph line 98 "*Try it: leave the signal at 3 Hz and drag the sample rate down below 6 Sa/s*" | The default shows the **non**-aliased case: no ghost curve, no folded spike, and the card says everything is fine. The tool is named "Sampling and aliasing" and its default state contains no aliasing; the closing paragraph has to instruct the reader to go and create the phenomenon. STYLE §Physics: "the default state must already show something worth looking at." | Default to `{ f: 7, fs: 10 }` — the ghost, the fold-back spike outside the shaded zone, and the amber card all appear immediately, and the reader can then drag *up* to fix it. |

---

## Minor

| # | File | Category | Exact line / function | What is wrong | Fix |
|---|------|----------|----------------------|---------------|-----|
| m1 | `cellular-reuse.html` | rendering | line 171 `.sort((p, r) => p.d - r.d).slice(0, 6);` | Always draws 6 interferer spokes even when the "Co-channel interferers" card says 2 (120° sectors) or 1 (60°). The section caption calls the ring "the first tier of co-channel interferers". | Slice to `interferers()` when `S.sec > 1`, or draw the sector-blocked ones dashed. |
| m2 | `cellular-reuse.html` | style | line 263 `$('cSn').innerHTML = '(&radic;3N)<sup>n</sup> / ' + interferers() + …` | Renders "(√3N)ⁿ", which reads as √3·N. The card footnote on line 72 correctly writes "√(3N)". | `(&radic;<span style="text-decoration:overline">3N</span>)` or just `(√(3N))ⁿ`. |
| m3 | `cellular-reuse.html` | rendering | line 222 `g.fillText(N, X(i), h - b + 6);` | Passes a number to `fillText`, and the x-axis is ordinal (index-spaced), not linear in N, so the SIR curve's shape is distorted relative to the axis the reader assumes. | `String(N)`; and either space the ticks by `L10(N)` or label the axis "cluster size N (not to scale)". |
| m4 | `fresnel-clearance.html` | style | lines 267–272 `$('h1v').textContent = S.h1 + ' m';` (and `h2v`, `oxv`, `ohv`) | Raw integers to the DOM; STYLE requires every displayed number through `.toFixed(n)`. Harmless today because the steps are integral, but it is the rule that stops the class of bug. | `.toFixed(0)`. |
| m5 | `fresnel-clearance.html` | style | line 163 `(S.k === 4 / 3 ? '4/3' : S.k === 1 ? '1' : S.k === 2 / 3 ? '2/3' : '1/2')` | Float equality on 4/3 and 2/3. Works only because the same literals are assigned on line 294. | Store the label alongside the value in the `KS` table. |
| m6 | `fresnel-clearance.html` | rendering | line 198 `g.moveTo(bx - 9, gBase); g.lineTo(bx, gTop); g.lineTo(bx + 9, gBase);` | The obstacle is a fixed ±9 px triangle regardless of the km scale, so a hill on an 80 km path is drawn the same width as on a 1 km path. | Scale the half-width with `(w - l - r) / S.d`. |
| m7 | `phased-array.html` | physics | line 194 `} else if (m.sl > 0.99) { $('mSL').textContent = 'grating lobe';` | 0.99 is an undisclosed threshold. Verified degenerate case: typing 0 into all three element boxes gives a flat pattern `af ≡ 1`, and the card then declares **"grating lobe"** for four elements stacked at one point. | Detect grating lobes from the geometry (element spacing > λ/(1+\|sin θ₀\|)) rather than from the sidelobe height, and short-circuit when `max(pos) - min(pos) < 1e-6`. |
| m8 | `phased-array.html` | pedagogy | line 285 `g.fillText(v.toFixed(2), l - 8, Y(v));` in `drawRect()` | The y-axis 0–1 has no title and no unit; only the section caption explains what 1.0 means. | Add a `'normalised field'` label in the left margin, as `radio-propagation.html` line 479 does with "dB per km". |
| m9 | `phased-array.html` | style | line 311 `$('frn').addEventListener('change', …)` | The number box uses `change` while its paired range uses `input`, so typing a frequency does nothing until blur. `phase-interferometry.html` line 391 has the same split. | Use `input` on both, or document the difference. |
| m10 | `phase-interferometry.html` | style | STYLE §Required structure item 9 | No closing `<p class="sub">` engineering-lesson paragraph; the document ends with the solver readout at line 95. Every other tool has one. | Add 3–5 sentences on why the short baseline resolves the long one and what a real system does when the jump ratio is too big. |
| m11 | `phase-interferometry.html` | numerics | line 176 `'true angle is ' + S.th + '° — a cycle count was picked wrong, the spacing jump is too big'` | When `sinEst` clamps at ±1 the recovered angle pins at ±90° and this message blames cycle counting, which may not be the cause. | Distinguish the clamp case and say the estimate ran off the visible hemisphere. |
| m12 | `phase-interferometry.html` | style | line 146 `$('thv').textContent = S.th + '°';` | Raw integer to DOM. | `.toFixed(0)`. |
| m13 | `antenna-basics.html` | physics | line 169 `if (Math.abs(thDeg - off) > 90) return 0.02;` (monopole); line 174 `return 0.06;` (patch); line 188 `const spill = t === 'dsh' ? 0.012 : 0.02;` | Undisclosed back-lobe floors that directly set the "Front-to-back ratio" card — the monopole's 34.0 dB is `20·log10(1/0.02)`, i.e. a typed-in constant presented as a measured ratio. | State the floors in the per-antenna notes ("ground plane assumed finite, back radiation −34 dB"), or model a finite ground plane. |
| m14 | `antenna-basics.html` | physics | line 153, dish note `Beamwidth ≈ 22/(F_GHz · D_m) degrees for a typical dish.` | A formula the reader cannot check: the tool has neither a frequency nor a diameter control, and the pattern uses a fixed `D = 11` wavelengths. | Add a diameter/frequency slider for the dish and drive `D` from it, so the note becomes verifiable. |
| m15 | `antenna-basics.html` | rendering | line 434 `g.fillText('electrical boresight', lx, ly - 4);` at 62 % of R along the peak | For narrow beams this label sits inside the shaded HPBW wedge and over the pattern outline; near ±90° it collides with the 90°/−90° axis tick labels drawn at `R + 18`. | Offset the label perpendicular to the peak direction, or place it outside R with a leader line. |
| m16 | `radio-propagation.html` | style | line 162 `function losStrength() { return fMHz() > 20 ? 1 : 0.25; }` | Dead code — defined and never called. | Delete. |
| m17 | `radio-propagation.html` | style | lines 574–582, the band buttons | Never receive the `.on` class, so there is no active-band indication, unlike every other button group in the suite. STYLE §Required structure item 6. | Set `.on` on click from `band().k`, and refresh it in `updateUI()` so slider drags keep it in sync. |
| m18 | `radio-propagation.html` | style | line 310 `g.fillText('✕ absorbed', dx + 30, dy);` | `✕` is a dingbat glyph; STYLE says no emoji, and it renders inconsistently across platforms. | Use the word "absorbed" with the existing coral colour, or draw the cross with two `lineTo` strokes. |
| m19 | `radio-propagation.html` | physics | line 387 `const fs = 32.44 + 20 * L10(Math.max(fMHz(), 0.01)) + 20 * L10(Math.max(d, 0.01));` | Friis goes negative at low frequency and short range — verified −58.0 dB at 3 kHz, 10 m — and the chart plots negative "path loss" against a gridline labelled "−40 dB". Physically the near field has been entered and the formula no longer applies. | Clamp the plotted domain to `d > lamM()/1000 * 10` (ten wavelengths) or floor the loss at 0 dB with a note. |
| m20 | `radio-propagation.html` | style | line 76 `<p class="sec"><b>1 &middot; …` vs lines 106 and 110 `<h2>2 &middot; …` | Section 1 is a `.sec` paragraph while sections 2 and 3 are `<h2>` — three sections, two different heading mechanisms in one document. | Use `.sec` for all three, per STYLE §Required structure item 7. |
| m21 | `modulation.html` | pedagogy | line 136 `const qbits = () => S.bits.length % 2 ? S.bits + '0' : S.bits;` | With an odd bit count the QPSK symbol boundaries in canvas 2 no longer line up with the bit dividers drawn in canvas 1 (which uses the unpadded `S.bits`), because the padded string is stretched over the same width. | Draw the message canvas from `qbits()` too when the mode is QPSK, and mark the pad bit. |
| m22 | `modulation.html` | style | lines 139–144 `'AM — the message rides in the HEIGHT of the carrier.'` etc. | Shouty capitals for emphasis; STYLE says sentence case everywhere. | Use `<b style="font-weight:500">` as `antenna-basics.html` line 129 does, and switch `$('note').textContent` to `innerHTML`. |
| m23 | `iq-constellation.html` | pedagogy | line 123 `const S = { scheme: 'bpsk', snr: 18, … }` and line 107 card `Symbol errors right now` | BPSK at 18 dB SNR is the most forgiving combination available — two tight blobs, 0.0 % errors, nothing moving. And "right now" is misleading: `regen()` only runs on scheme/SNR change, so the 400 noise samples are frozen and the error count never varies until a slider moves. | Default to QPSK at 11–12 dB so clusters visibly blur; and either re-roll a few symbols per frame or rename the card "symbol errors in this capture". |
| m24 | `.research/STYLE.md` | style | line 15 "the standard `<style>` block (tokens, `body`, `h1`, `.sub`, `.back`, `button`, `.row`, `.ctl`, …)" vs `link-budget.html` line 30 `.grid2 { … }` | The contract names `.row` as a required class, but the reference implementation it tells you to copy verbatim has no `.row` at all — it uses `.grid2` + `.ctl`. Half the suite followed the contract, half followed the file, so the two layout idioms are now mixed across tools. | Decide which is canonical and edit STYLE.md to match `link-budget.html`, since that file is normative by §Reference implementation. |

---

## Notes on what is correct

Worth recording so a future pass does not "fix" working code:

- `link-budget.html` line 114 `92.4 + 20*L10(S.f) + 20*L10(d)` and line 115 `-174 + 10*L10(S.bw*1e6) + S.nf` — both standard and correct (32.44 + 60 for GHz; kT₀ = −174 dBm/Hz).
- `fourier-series.html` / `epicycles.html` `coef(n)` — square 4/(πn) odd, sawtooth 2/(πn)·(−1)ⁿ⁺¹, triangle 8/(π²n²)·(−1)^((n−1)/2). All three verified against their `target()` definitions.
- `phase-interferometry.html` line 108 `360 * p * Math.sin(S.th*π/180) / lam()` and the `solve()` cycle-count walk — correct, and the geometry drawing uses one isotropic `scale` for both axes, so the aspect-ratio bug named in the brief is **not** present here.
- `aoa-triangulation.html` line 113 `Math.atan2((S.ex - st.x) * KMX, (st.y - S.ey) * KMY)` and `proj()` lines 143–147 — bearings are computed *and* projected in km space before the anisotropic map to pixels. This is the correct handling of the repo's known aspect-ratio bug.
- `tdoa.html` `drawHyperbola` — correct signed single-branch parameterisation, with `Math.abs(aS) >= cH - 1e-6` guarding the `sqrt`; and `/ 0.299792458` is c in km/µs.
- `phased-array.html` `af()` — textbook array factor; `cellular-reuse.html` `q()` = √(3N) and `sirLin()` = qⁿ/i₀ with i₀ = 6/2/1 — all correct.
- `fresnel-clearance.html` `F1` = 17.3√(d₁d₂/(f·d)), `bulge` = d₁d₂·1000/(2kRₑ) (= d₁d₂/12.74), and ν = −√2·clearance/F₁ — all correct; only the cutoffs around them are not.
- `radio-propagation.html` `horizonKm` = 4.12(√h₁+√h₂), the MUF secant law `foF2()/sin(15°)` = foF2·sec(75°), and the P.838 rain coefficients k = 4.21e−5·f^2.42, α = 1.41·f^−0.0779 — genuine published values (though applied well outside their 3–54 GHz / 8.5–25 GHz validity ranges).
- `iq-constellation.html` line 154 `Math.pow(10, -S.snr/20)/Math.sqrt(2)` — correct for unit-Es constellations under an Es/N_total convention; all four `ideal()` sets are correctly normalised to Es = 1.
- `modulation.html` FSK and FM use phase accumulation, so they are continuous-phase — the right choice, and the QPSK map is Gray-coded.
